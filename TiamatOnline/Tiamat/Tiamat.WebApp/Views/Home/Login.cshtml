@{
    ViewData["Title"] = "Вход";
}

<style>
    :root {
        --primary-color: #4361ee;
        --primary-hover: #3a56d4;
        --secondary-color: #7209b7;
        --bg-gradient: linear-gradient(135deg, #4361ee, #7209b7);
        --text-color: #ffffff;
        --light-text: #f8f9fa;
        --card-bg: rgba(28, 28, 30, 0.95);
        --input-bg: rgba(40, 40, 45, 0.9);
        --shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
        --border-radius: 12px;
        --transition: all 0.3s ease;
        --dark-bg: #121212;
        --darker-bg: #0a0a0a;
    }

    .d-flex {
        display: flex;
    }

    .flex-column {
        flex-direction: column;
    }

    .justify-content-center {
        justify-content: center;
    }

    .align-items-center {
        align-items: center;
    }

    .flex-grow-1 {
        flex-grow: 1;
    }

    .mt-5 {
        margin-top: 3rem;
    }

    main {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        min-height: 80vh;
        width: 100%;
        max-width: 1440px;
        margin: 0 auto;
        padding: 0 2rem;
        gap: 3rem;
    }

    @@media (min-width: 1600px) {
        main {
            max-width: 1600px;
        }
    }

    @@media (max-width: 1200px) {
        main {
            padding: 0 1.5rem;
        }
    }

    @@media (max-width: 992px) {
        main {
            flex-direction: column;
            gap: 2rem;
            min-height: auto;
            padding-top: 2rem;
            padding-bottom: 2rem;
        }
    }

    .explanation-container {
        flex: 1;
        max-width: 640px;
        animation: fadeInLeft 0.8s ease-out;
    }

    .explanation-text {
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        font-weight: 600;
        line-height: 1.3;
        color: var(--text-color);
    }

    @@media (max-width: 1200px) {
        .explanation-text {
            font-size: 2rem;
        }
    }

    .beta-container {
        background: rgba(22, 22, 26, 0.7);
        border: 1px solid rgba(255, 255, 255, 0.05);
        border-radius: var(--border-radius);
        padding: 1.5rem;
        position: relative;
        overflow: hidden;
        margin-top: 1.5rem;
    }

        .beta-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--bg-gradient);
        }

    .beta-badge {
        display: inline-block;
        background-color: rgba(67, 97, 238, 0.2);
        border: 1px solid rgba(67, 97, 238, 0.5);
        color: var(--primary-color);
        padding: 0.25rem 0.75rem;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
        margin-bottom: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        animation: floating 3s ease-in-out infinite;
    }

    .beta-container h3 {
        font-size: 1.5rem;
        margin-top: 0;
        margin-bottom: 1rem;
        color: var(--text-color);
    }

    .beta-container p {
        margin-bottom: 0.5rem;
        line-height: 1.6;
        color: rgba(255, 255, 255, 0.8);
        font-size: 1rem;
    }

    .login-container {
        flex: 0 0 450px;
        background: var(--card-bg);
        padding: 2.5rem;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        animation: fadeInRight 0.8s ease-out;
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.05);
    }

        .login-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--bg-gradient);
        }

    .login-title {
        font-size: 1.75rem;
        text-align: center;
        margin-bottom: 2rem;
        font-weight: 600;
        color: var(--text-color);
    }

    .input-group {
        position: relative;
        margin-bottom: 1.5rem;
    }

        .input-group input {
            width: 100%;
            padding: 1rem 1rem 1rem 3rem;
            background-color: var(--input-bg);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            font-size: 1rem;
            transition: var(--transition);
            box-sizing: border-box;
            color: var(--text-color);
        }

            .input-group input:focus {
                border-color: var(--primary-color);
                box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.15);
                outline: none;
            }

            .input-group input::placeholder {
                color: rgba(255, 255, 255, 0.5);
            }

    .input-icon {
        position: absolute;
        left: 1rem;
        top: 50%;
        transform: translateY(-50%);
        color: rgba(255, 255, 255, 0.5);
        transition: var(--transition);
    }

    .input-group input:focus + .input-icon {
        color: var(--primary-color);
    }

    button[type="submit"] {
        width: 100%;
        padding: 0.9rem;
        background: var(--bg-gradient);
        color: white;
        border: none;
        border-radius: 8px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: var(--transition);
        margin-top: 0.5rem;
        letter-spacing: 1px;
    }

        button[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(67, 97, 238, 0.2);
        }

    .checkbox-container {
        display: flex;
        align-items: center;
        margin-top: 1.5rem;
    }

        .checkbox-container input[type="checkbox"] {
            margin-right: 0.5rem;
            cursor: pointer;
            accent-color: var(--primary-color);
        }

        .checkbox-container label {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
            cursor: pointer;
        }

    @@keyframes fadeInLeft {
        from {
            opacity: 0;
            transform: translateX(-30px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @@keyframes fadeInRight {
        from {
            opacity: 0;
            transform: translateX(30px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @@keyframes floating {
        0% {
            transform: translateY(0px);
        }

        50% {
            transform: translateY(-6px);
        }

        100% {
            transform: translateY(0px);
        }
    }
</style>

<div class="d-flex flex-column">
    <main class="d-flex justify-content-center align-items-center flex-grow-1 mt-5">
        <div class="explanation-container">
            <h1 class="explanation-text">Добре дошли в Tiamat! Моля, влезте за да продължите.</h1>

            <div class="beta-container">
                <span class="beta-badge">ЗАТВОРЕНА БЕТА</span>
                <h3>Ексклузивен достъп</h3>
                <p>В момента Tiamat е в затворена бета версия, достъпна само за избрани потребители. Публично достъпна е само страницата за вход.</p>
                <p>Благодарим ви за интереса! Работим усилено, за да предоставим пълната функционалност скоро.</p>
            </div>
        </div>

        <div class="login-container">
            <h2 class="login-title">ВХОД</h2>
            <form method="post" asp-action="Login" asp-controller="Home">
                <div class="input-group">
                    <input type="text" name="username" placeholder="Потребителско име" required>
                    <span class="input-icon">
                        <i class="bi bi-person"></i>
                    </span>
                </div>
                <div class="input-group">
                    <input type="password" name="password" placeholder="Парола" required>
                    <span class="input-icon">
                        <i class="bi bi-lock"></i>
                    </span>
                </div>
                <div class="checkbox-container">
                    <input type="checkbox" id="rememberMe" name="rememberMe">
                    <label for="rememberMe">Запомни ме</label>
                </div>
                <button type="submit">ВХОД</button>
            </form>
        </div>
    </main>
</div>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css">